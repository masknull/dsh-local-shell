# dsh-local-shell 构建不需要安装 peerDependencies(它们是插件包给 DSH 宿主
# 注入的运行时依赖, 且 @deepseek-ai/dsh-tools 依赖未发布的 dsh-type-meta,
# npm 上 404)。前端 tsc/vite 只消费 dependencies + devDependencies。
auto-install-peers=false
# vite 依赖 esbuild 的 postinstall(平台二进制), pnpm 11 默认拦截构建脚本。
dangerously-allow-all-builds=true